component Divider {
  /* Styles for the root element. */
  style root {
    border-top: 2px dashed #CCC;
    margin: 100px 0;
  }

  /* Renders the component. */
  fun render : Html {
    <div::root/>
  }
}
