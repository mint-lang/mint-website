module Guides {
  const CLI_VERSION =
    <<#MARKDOWN(highlight)
    # Version

    The `version` command shows the version of the executable.

    ```bash
    ➔ mint version
    Mint 0.20.0
    ```

    ## Help

    ```bash
    ➔ mint version --help
    Usage:
      mint test [flags...] [arg...]

    Shows version.

    Flags:
      --help  # Displays help for the current command.
    ```
    MARKDOWN
}
