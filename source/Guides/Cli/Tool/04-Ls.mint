module Guides {
  const CLI_TOOL_LS =
    <<#MARKDOWN(highlight)
    # Ls

    The `ls` tool starts the language server which editors can integrate with.

    ## Help

    ```bash
    ➔ mint tool ls --help
    Usage:
      mint tool ls [flags...] [arg...]

    Starts the language server process.

    Flags:
      --help  # Displays help for the current command.
    ```
    MARKDOWN
}
